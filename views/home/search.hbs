<div class="container py-5">
  <div class="row">
    {{#each users as |u|}}
    {{query}}
      <div class="col-lg-3 col-md-6 col-sm-12 px-2">
        <div class="card">
          <div class="mt-3 mb-4 mx-auto w-fit-content">
            <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-chat/ava2-bg.webp"
              class="rounded-circle img-fluid d-block" style="width: 100px;" />
              {{!-- {{#if true}}
                <div class="active-circle">
                </div>
              {{/if}} --}}
            {{!-- <p class="fw-bold my-1 text-center">{{u.name}}</p> --}}
            <a href="/{{u.username}}" class="d-block h5 card-title text-center text-primary fw-bold">@{{u.username}}</a>

          </div>
          <div class="card-body">
            <div class="d-flex justify-content-space-around">
              <p class="fw-bold my-1 text-center">{{u.name}}</p>
              {{!-- <a href="/{{u.username}}/add-friend?next=?search={{../query}}" class="btn btn-primary">add friend<i class="bi bi-check"></i></a>
              <a href="/chat/" class="btn btn-primary"><i class="bi bi-chat-fill"></i>chat</a> --}}
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>